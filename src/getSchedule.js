const data = require('../data/zoo_data');
// const {data1} = data;
// const obj = Object.keys(data1);
// console.log(obj);
function getSchedule(scheduleTarget) {
//   return data.hours.reduce((acc, days) => {
//     acc[days] = {'officeHour': 'Open from 8am until 6pm,'}
//     // acc[days] = {'exhibition': 'Open from 8am until 6pm',}
//     return acc
//   }, {})
}
// console.log(getSchedule());

// return getAnimal(scheduleTarget)
// function getAnimal(animal) {
//   const ni = data.species.find((an) => an.name === animal)
//   if (!ni) {
//     return false;
//   } return ni.availability;
// }
// console.log(getSchedule('lions'));

module.exports = getSchedule;
